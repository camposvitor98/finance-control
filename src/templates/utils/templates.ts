import { HomeTemplate } from "templates/HomeTemplate";

export const templates = {
  home: {
    template: HomeTemplate,
    props: {},
  },
  login: {
    template: HomeTemplate,
    props: {},
  },
};
